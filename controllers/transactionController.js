// purpose of file: all functions that controls transactions should be written here


const Transactions = require('./../models/transaction')
// you can import any other requred model if needed

const mongoose = require('mongoose')
// you can import any other package needed as well



const sampleFunc = async () => {
    // your function
}


// your other func goes down here







// to clearify you can write as many func as you need, it's not just one func 













module.exports = {
    sampleFunc,
    // your other funcs too
}